# Transformer_Architecture
I will be unrolling the main components of a traditional transformer including encoder, decoder model unwrapping the self attention mechanism, the feed forward network layers, layer normalization, regularization, residual connections using sublayers and the encoder/ decoder layer architectures
